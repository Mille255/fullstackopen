{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rimraf dist && cd ../part2/puhelinluettelo && npm run build && cpy dist ../../part3 --recursive",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"deploy backend and frontend\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mika Huhtala",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "cpy-cli": "^6.0.0",
    "rimraf": "^6.1.0"
  }
}
