{
  "name": "phonebookbackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rimraf dist && cd ../part2/puhelinluettelo && npm run build && cpy dist ../../part3 --recursive",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"3.21 tietokantaa käyttävä versio Internettiin\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mika Huhtala",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.3",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "cpy-cli": "^6.0.0",
    "rimraf": "^6.1.0"
  }
}
